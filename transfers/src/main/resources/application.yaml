spring:
  application:
    name: transfers
  datasource:
    url: jdbc:postgresql://ep-little-king-ay9m7fgu-pooler.c-5.us-east-2.aws.neon.tech/BankDB?sslmode=require&channel_binding=require
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          jwk-set-uri: http://localhost:8110/realms/banktransferidempotency/protocol/openid-connect/certs

server:
  port: 8081
logging:
  level:
    org.springframework.security: trace